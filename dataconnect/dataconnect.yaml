specVersion: "v1beta"
serviceId: "functions"
location: "us-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "functions-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
